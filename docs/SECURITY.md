# ضوابط أمن المعلومات — تُجّار ماركت

وثيقة مرجعية لضوابط الأمان المطبّقة فعلياً في الكود، تُستخدم ضمن ملف التقديم للاختبار الفني لدى الهيئة الناظمة
للاتصالات والبريد. كل بند يشير إلى مكان تطبيقه.

الحالة: ✅ مطبّق ومختبر · 🟡 مطبّق ويحتاج إعداد الإنتاج · ⏳ مخطط

---

## 1. المعمارية

- واجهة الزبائن وبوابة التجار ولوحة الإدارة تتواصل مع API واحد (NestJS)، ولكل واجهة جلسة مستقلة
- الواجهات الثلاث تطبيقات منفصلة: كل بناء لا يحتوي إلا صفحاته والصفحات المشتركة (الشروط، الخصوصية)،
  فكود لوحة الإدارة لا يُرسل أبداً إلى متصفح الزبون أو التاجر (`apps/web/next.config.ts`، `pageExtensions`)
- قاعدة البيانات PostgreSQL ومخزن الصور (MinIO / S3) غير مكشوفين مباشرة للمستخدم
- كل عمليات الكتابة تمر عبر API مع تحقق من الهوية والصلاحية والمدخلات

## 2. الهوية والجلسات

| الضابط | الحالة | التطبيق |
|---|---|---|
| تحقق رقم الموبايل برمز SMS عند التسجيل واستعادة كلمة المرور | 🟡 يحتاج مزوّد SMS | `apps/api/src/auth/otp.service.ts` |
| رموز التحقق: 6 أرقام عشوائية آمنة، صلاحية 5 دقائق، 5 محاولات، تخزين HMAC وليس الرمز | ✅ | `otp.service.ts` |
| حد لطلب الرموز: دقيقة بين كل طلبين و5 رموز بالساعة لكل رقم | ✅ | `otp.service.ts` |
| استعادة كلمة المرور لا تكشف إن كان الرقم مسجلاً | ✅ | `otp.service.ts` |
| كلمات المرور: bcrypt بتكلفة 12، و8 أحرف على الأقل مع حروف وأرقام | ✅ | `auth.service.ts`، `auth.dto.ts` |
| قفل الحساب 15 دقيقة بعد 5 محاولات فاشلة | ✅ | `auth.service.ts` |
| زمن استجابة ثابت للرقم غير الموجود (منع كشف الحسابات) | ✅ | `auth.service.ts` |
| رمز وصول JWT لمدة 15 دقيقة في كوكي httpOnly + Secure + SameSite | ✅ | `auth/cookies.ts` |
| رمز تجديد عشوائي يُخزَّن مجزّأً (SHA-256) ويتبدّل مع كل استخدام | ✅ | `auth/session.service.ts` |
| كشف إعادة استخدام رمز التجديد وإلغاء كل جلسات العائلة | ✅ | `session.service.ts` |
| جلسات منفصلة لكل واجهة: الدور لا يدخل واجهة غيره | ✅ | `auth/roles.ts`، `jwt.strategy.ts` |
| تسجيل الخروج يلغي الجلسة من الخادم | ✅ | `auth.controller.ts` |
| تغيير كلمة المرور يلغي كل الجلسات | ✅ | `auth.service.ts` |

## 3. الإدارة

| الضابط | الحالة | التطبيق |
|---|---|---|
| مصادقة ثنائية TOTP إلزامية لكل فريق الإدارة قبل أي عملية | ✅ مختبر بمتجهات RFC 6238 | `auth/totp.ts`، `guards.ts` |
| سر المصادقة مشفّر AES-256-GCM في قاعدة البيانات | ✅ | `common/crypto.ts` |
| منع إعادة استخدام نفس رمز المصادقة | ✅ | `auth.service.ts` |
| جلسة الإدارة 12 ساعة، وفحص إلغائها مع كل طلب | ✅ | `roles.ts`، `jwt.strategy.ts` |
| أدوار: مدير، مشرف، مندوب ميداني. إيقاف المتاجر للمدير فقط | ✅ | `admin.controller.ts` |
| سجل تدقيق لكل عمليات الدخول والتوثيق والمراجعة والبلاغات | ✅ | `audit/audit.module.ts` |
| لوحة الإدارة تطبيق منفصل على نطاق خاص، والـ API لا يقبل جلسة الإدارة إلا من نطاقها | ✅ محلياً · 🟡 النطاق الفرعي عند النشر | `ADMIN_ORIGIN`، `csrf.guard.ts` |
| استعادة كلمة المرور بالـ SMS غير متاحة لحسابات الإدارة | ✅ | `reset-password/page.shared.tsx` |

## 4. حماية الطلبات

| الضابط | الحالة | التطبيق |
|---|---|---|
| حماية CSRF: ترويسة `X-Client` إلزامية + فحص Origin + كوكيز SameSite | ✅ | `common/csrf.guard.ts` |
| CORS مقيّد بنطاقات الواجهات فقط | ✅ | `main.ts` |
| تحديد معدل الطلبات عاماً (120/دقيقة) ومشدداً على الدخول والتسجيل والرموز والبلاغات، مع ترويسات `RateLimit-*` و`Retry-After` | ✅ | `common/throttle.ts` + `@Throttle` |
| العدادات في ذاكرة الخادم؛ التشغيل على أكثر من نسخة يحتاج مخزناً مشتركاً (Redis) | 🟡 عند التوسع | `common/throttle.ts` |
| تحقق صارم من كل المدخلات، ورفض الحقول غير المعروفة | ✅ | `ValidationPipe` في `main.ts` |
| حجم الطلب محدود (200KB) | ✅ | `main.ts` |
| رسائل خطأ عامة دون تفاصيل داخلية، مع تسجيلها على الخادم | ✅ | `common/filters/all-exceptions.filter.ts` |
| استعلامات قاعدة البيانات عبر Prisma (استعلامات مُعدّة، لا نصوص SQL مركّبة) | ✅ | كل الخدمات |
| مجاميع الطلبات تُخزَّن BigInt، فلا تنكسر مع الطلبات الكبيرة (سعر حتى 2 مليار × كمية حتى 9999)، وتُحوَّل إلى أرقام عادية قبل الإرسال | ✅ مختبر | `orders/orders.ts`، `prisma/schema.prisma` |
| إعداد الخادم (الترويسات، الكوكيز، حدود الحجم، التحقق) في دالة واحدة تستعملها الاختبارات كما يستعملها التشغيل | ✅ | `app-setup.ts` |

## 5. ترويسات الأمان

| الضابط | الحالة | التطبيق |
|---|---|---|
| API: Helmet (CSP صارم، منع التضمين، nosniff، إلخ) | ✅ | `apps/api/src/main.ts` |
| الواجهة: Content-Security-Policy، X-Frame-Options: DENY، nosniff، Referrer-Policy، Permissions-Policy | ✅ | `apps/web/next.config.ts` |
| HSTS مع preload | 🟡 يُفعَّل تلقائياً في الإنتاج | `next.config.ts` |
| إخفاء ترويسة X-Powered-By | ✅ | الطرفين |
| بوابة التجار ولوحة الإدارة: `X-Robots-Tag: noindex, nofollow` لمنع فهرستها | ✅ | `next.config.ts` |

## 6. الملفات والصور

| الضابط | الحالة | التطبيق |
|---|---|---|
| فحص نوع الملف من محتواه الحقيقي وليس من امتداده | ✅ | `media/media.service.ts` |
| إعادة ترميز كل صورة إلى WebP (يزيل أي محتوى مدسوس) | ✅ | `media.service.ts` |
| حذف كل البيانات المخفية بما فيها موقع GPS | ✅ | `media.service.ts` |
| حد للحجم (8MB) وللأبعاد (40 ميغابكسل) | ✅ | `media.service.ts` |
| الرفع للتجار الموثّقين فقط ومعدل محدود | ✅ | `media.controller.ts` |
| وثائق التوثيق (الهوية وفيديو المحل) في مخزن خاص بلا أي صلاحية عامة، ومشفّرة داخل الـ API بـ AES-256-GCM قبل التخزين | ✅ مختبر | `verification/kyc-storage.service.ts` |
| الوثائق لا تُعرض إلا للمشرفين عبر الـ API بجلسة إدارة ومصادقة ثنائية، بلا تخزين مؤقت، وكل اطلاع مسجّل | ✅ مختبر | `verification.controller.ts` |
| حذف ملفات الطلبات المرفوضة بعد 30 يوماً | ✅ | `verification.service.ts` |

## 6ب. توثيق التجار (نموذج متدرّج حسب المخاطر)

| الضابط | الحالة | التطبيق |
|---|---|---|
| تعهّد إلزامي بصحة معلومات المتجر عند التسجيل مع حفظ وقته (DSA المادة 30) | ✅ مختبر | `auth.dto.ts`، `Store.attestedAt` |
| حدود منتجات حسب مستوى التوثيق: 10 ثم 50 ثم بلا حدود | ✅ مختبر | `products.service.ts` |
| صور الهوية تُعاد ترميزها وتُحذف بياناتها الوصفية، ونوع الفيديو يُفحص من محتواه | ✅ مختبر | `verification.service.ts` |
| فيديو المحل يُصوَّر مباشرة من الصفحة، ويُرفض إذا مضى على تصويره أكثر من 20 دقيقة، أو كانت دقة GPS أسوأ من 150 م، أو كان خارج سوريا أو خارج حدود السوق | ✅ مختبر | `verification.service.ts`، `ShopVideoForm.tsx` |
| طلب واحد فقط قيد المراجعة لكل نوع، ومحمي من الطلبات المتزامنة بمعاملة Serializable | ✅ | `verification.service.ts` |
| قرار المراجعة يُتخذ مرة واحدة، والرفض يتطلب سبباً يظهر للتاجر | ✅ مختبر | `verification.service.ts` |
| رفع المستوى لا يتم إلا بأدلة التاجر ومراجعتها؛ «تاجر مميز» بعد زيارة ميدانية وبملاحظة إلزامية | ✅ مختبر | `setStoreLevel` |
| تغيير اسم المتجر أو سوقه يلغي توثيق المحل تلقائياً | ✅ مختبر | `stores.service.ts` |
| 3 بلاغات مؤكدة خلال 90 يوماً توقف الشارة تلقائياً حتى يستعيدها مدير | ✅ مختبر | `applyReportThreshold` |
| انتهاء توثيق المحل بعد سنة (مهمة دورية كل 6 ساعات) | ✅ | `runMaintenance` |
| حدود جغرافية دقيقة لكل أسواق دمشق | ⏳ المرحلة 4 | `Market.latitude/longitude/radiusMeters` |
| إحداثيات GPS يرسلها الموبايل ويمكن تزويرها بتطبيقات خاصة، لذلك يبقى الفيديو ومراجعة المشرف والبلاغات خط الدفاع الأساسي | ملاحظة | — |

## 6ج. الإطلاق التدريجي والأسواق

| الضابط | الحالة | التطبيق |
|---|---|---|
| المتاجر لا تُسجَّل ولا تظهر للزبائن إلا في المحافظات المفتوحة، ولا يمكن نقل متجر إلى محافظة غير مفتوحة | ✅ مختبر | `selects.ts` (`publicStoreWhere`)، `auth.service.ts`، `stores.service.ts` |
| الحدود الجغرافية المبدئية لا ترفض أحداً؛ الرفض التلقائي للحدود المؤكدة فقط، والتأكيد للمدير وحده | ✅ مختبر | `verification.service.ts`، `markets.service.ts` |
| رفض إحداثيات خارج سوريا ونصف قطر خارج 50–3000 م | ✅ مختبر | `markets.dto.ts`، `common/geo.ts` |
| لا يُحذف سوق فيه متاجر ولا قسم فيه منتجات؛ التعطيل بديل آمن | ✅ مختبر | `markets.service.ts` |
| القسم المعطّل لا يُختار لمنتجات جديدة، والسوق المعطّل لا يُختار لمتاجر جديدة | ✅ مختبر | `products.service.ts`، `auth.service.ts` |
| طلب الاهتمام العام محدود بـ 5 طلبات/ساعة لكل IP، ولا يكشف إن كان الرقم مسجلاً، ولا يُكتب الرقم في سجل التدقيق | ✅ مختبر | `markets.controller.ts` |
| كل تغيير في المحافظات والأسواق والحدود والأقسام ومتابعة التجار مسجّل في سجل التدقيق؛ التعديل للمدير والاطلاع للمشرف | ✅ مختبر | `markets.controller.ts` |

## 6د. الإشعارات (داخل التطبيق وWeb Push)

| الضابط | الحالة | التطبيق |
|---|---|---|
| الخادم لا يرسل إشعارات إلا إلى خدمات الدفع الحقيقية (Google وMozilla وApple وMicrosoft) عبر HTTPS؛ أي عنوان آخر مرفوض، فلا يمكن استغلال الاشتراك لطلبات من الخادم إلى عناوين داخلية (SSRF) | ✅ مختبر | `push.service.ts` (`assertPushEndpoint`) |
| مفتاح VAPID الخاص يُولَّد مرة واحدة ويُحفظ مشفّراً (AES-256-GCM)، أو يُعطى من متغيرات البيئة | ✅ | `push.service.ts`، `AppSetting` |
| كل مستخدم يرى ويعدّل إشعاراته فقط، والإشعار يذهب إلى أجهزة الواجهة التي يدخل منها صاحبه فقط | ✅ مختبر | `notifications.service.ts` |
| روابط الإشعارات والحملات صفحات داخل المنصة فقط (تبدأ بـ / ولا تبدأ بـ //)، والـ Service Worker يتحقق منها مرة أخرى، فلا تُستعمل للتصيّد | ✅ مختبر | `campaigns.ts`، `public/sw.js` |
| الحملات للمدير وحده، بحد 10 حملات/ساعة، وكل حملة في سجل التدقيق مع جمهورها وعدد المستهدفين | ✅ مختبر | `campaigns.ts` |
| احترام اختيار المستخدم: إيقاف نوع لا يصله، والتسويقية على الجهاز نهاراً فقط وبحد 3 يومياً | ✅ مختبر | `categories.ts`، `notifications.service.ts` |
| الاشتراكات المنتهية (404/410) تُحذف تلقائياً، وبحد 10 أجهزة لكل مستخدم، وتُفصل عن الحساب عند تسجيل الخروج | ✅ | `push.service.ts`، `lib/session.ts` |
| نص الإشعار لا يحتوي أرقام هواتف أو بيانات حساسة، وملاحظات المشرف الداخلية لا تُرسل للزبون | ✅ | خدمات الأحداث |
| المفضلة والمتابعة للزبون المسجّل فقط، بحد 200 منتج و500 متجر | ✅ مختبر | `engagement.ts` |

## 6هـ. الحساب والإدارة (المرحلة 8)

| الضابط | الحالة | التطبيق |
|---|---|---|
| تغيير رقم الموبايل يتطلب كلمة المرور الحالية ورمزاً يُرسل إلى الرقم الجديد، ويُرفض الرقم المستخدم في حساب آخر، وتُنهى جلسات الأجهزة الأخرى، ويُنبَّه الرقم القديم برسالة | ✅ | `auth.service.ts` (`changePhone`) |
| طلب رمز تغيير الرقم للمستخدم المسجّل فقط وبحد 5 طلبات/ساعة، فلا يُستغل لفحص الأرقام | ✅ | `auth.controller.ts` |
| إدارة المستخدمين: الاطلاع للمشرف، والإيقاف وفك القفل للمدير فقط؛ لا يعدّل المدير حسابه ولا حسابات المدراء الآخرين؛ الإيقاف يتطلب سبباً وينهي كل الجلسات فوراً؛ كل إجراء في سجل التدقيق | ✅ | `admin/users-stats.ts` |
| البيانات المنظّمة (JSON-LD) تُهرَّب قبل إدراجها في الصفحة فلا يغلق نص المنتج وسم السكربت | ✅ | `components/seo/JsonLd.tsx` |
| الخرائط من OpenStreetMap بلا مفتاح، ومسموح نطاق البلاطات فقط في سياسة أمان المحتوى | ✅ | `next.config.ts` |

## 7. البيانات والخصوصية

| الضابط | الحالة | التطبيق |
|---|---|---|
| موافقة صريحة على الشروط وسياسة الخصوصية مع حفظ الإصدار والتاريخ | ✅ | `auth.dto.ts`، `User.termsVersion` |
| صفحات الشروط والأحكام وسياسة الخصوصية | 🟡 تحتاج مراجعة قانونية | `apps/web/src/app/terms`، `privacy` |
| هوية المُبلِّغ لا تظهر إلا للإدارة | ✅ | `reports.module.ts`، `admin.service.ts` |
| الأسرار في متغيرات بيئة، والتشغيل يفشل إذا غابت في الإنتاج | ✅ | `apps/api/src/env.ts` |
| قاعدة البيانات في مجلد تخزين Docker، غير مكشوفة للإنترنت | 🟡 عند النشر | `infra/docker-compose.yml` |
| نسخ احتياطي يومي مشفّر واختبار الاسترجاع | ⏳ المرحلة 9 | — |

## 8. المكتبات

| الضابط | الحالة |
|---|---|
| الواجهة: Next.js 16.3.5، و`npm audit` بلا أي ثغرة (إنتاج وتطوير) | ✅ |
| الـ API: NestJS 12.0.3 (multer 2.4.0، Express 5.2.1) وPrisma 6.19.3، و`npm audit` بلا أي ثغرة (إنتاج وتطوير) | ✅ |
| `deepmerge-ts` مثبّتة على 8.0.2 عبر `overrides` في `package.json` لإغلاق ثغرة أداة إعداد Prisma | ✅ |
| فحص تلقائي للمكتبات في GitHub Actions | ⏳ |

**ملاحظات الترقية إلى NestJS 12:**
- المشروع يبقى CommonJS ويحمّل حزم Nest (ESM فقط) عبر `require(esm)`، ويتطلب Node.js 22.12 أو أحدث.
- `@nestjs/throttler` لا يدعم NestJS 12 بعد، فاستُبدل بحارس داخلي مكافئ ومختبر (`common/throttle.ts`).
- أداة `@nestjs/cli` بقيت على الإصدار 11 للبناء، لأن إصدارها 12 يتطلب TypeScript 6 وNode.js 22.22.3 أو أحدث.
- أُعيد تعريف `@Optional()` في `JwtAuthGuard`، لأن NestJS 12 لم يعد يورّثه من الفئة الأم.

## 9. نتائج الاختبارات (15 أيلول 2026، بيئة التطوير)

| مجموعة الاختبار | النتيجة |
|---|---|
| المصادقة الثنائية والتشفير: متجهات RFC 4226 وRFC 6238، الانحراف الزمني، رفض الرموز القديمة، كشف التلاعب في AES-GCM | 11 / 11 ✅ |
| اختبار أمني شامل على الـ API: الترويسات، CSRF، رموز SMS، الشروط، سياسة كلمات المرور، خصائص الكوكيز، فصل الواجهات، تبديل الجلسات وكشف إعادة الاستخدام، البلاغات، الرفع وإعادة الترميز، المصادقة الثنائية للإدارة، سجل التدقيق، حد محاولات الدخول | 40 / 40 ✅ |
| قفل الحساب: عدّ المحاولات، القفل 15 دقيقة، رفض كلمة المرور الصحيحة أثناء القفل، تسجيله في سجل التدقيق | 8 / 8 ✅ |
| فصل الواجهات: كل صفحة تعمل على واجهتها فقط وتعطي 404 في غيرها، مع ترويسة منع الفهرسة (54 فحصاً على 3 نطاقات) | 54 / 54 ✅ |
| التوثيق المتدرّج للتجار: التعهد، حدود المنتجات، الهوية وفيديو المحل، التشفير في المخزن الخاص، المراجعة، تأكيد حدود السوق وتطبيقها، إلغاء التوثيق عند النقل، إيقاف الشارة بالبلاغات، سجل التدقيق | 45 / 45 ✅ |
| الإطلاق التدريجي والأسواق: حالة المحافظات، اهتمام التجار، إدارة الأسواق والأقسام، الحدود الجغرافية، إخفاء متاجر المحافظات غير المفتوحة، صلاحيات التعديل، سجل التدقيق | 32 / 32 ✅ |
| التقييمات ومصداقية المُبلِّغين (16 أيلول): شرط التواصل الفعلي ومهلته، تقييم واحد لكل زبون، حجز التعليقات المشبوهة، إخفاء هوية الزبون عن العامة والتاجر، رد التاجر وطلب المراجعة، قرارات الإدارة بسبب إلزامي، منع تقييم المتجر الذاتي، إيقاف المُبلِّغ الكيدي، سجل التدقيق | 32 / 32 ✅ |
| الإشعارات (17 أيلول): مركز الإشعارات والعدّاد، الإعدادات، رفض عناوين الدفع غير الحقيقية، الاشتراك وإلغاؤه، المتابعة والمفضلة، المنتجات الجديدة المجمّعة، انخفاض السعر والتوفر، عدم التكرار، احترام الإيقاف، عزل الحسابات، التقييم والرد، طوابير المشرفين، نتيجة البلاغ، إيقاف المتجر، الحملات وروابطها وصلاحياتها وسجل التدقيق | 31 / 31 ✅ |
| بناء الواجهات الثلاث للإنتاج كلٌّ على حدة، وفحص الأنواع للطرفين | ✅ |

> ملاحظة تشغيل: عند إعادة الاختبار الأمني الشامل مباشرة بعد تجارب دخول أخرى، قد يعطي فحص «Failed logins return 401»
> الرمز 429 لأن حد محاولات الدخول لعنوان IP (10 كل 15 دقيقة) استُهلك — وهذا سلوك الحماية نفسه. أعد تشغيل الـ API ثم أعد الاختبار.

## 10. قبل التقديم للهيئة

- [ ] مزوّد SMS فعلي بدل مُرسل التطوير
- [ ] نشر على HTTPS بنطاقات منفصلة للواجهات الثلاث
- [ ] أسرار إنتاج عشوائية جديدة (JWT, OTP_PEPPER, TOTP_ENC_KEY) وكلمات مرور جديدة لقاعدة البيانات وMinIO
- [ ] حذف حسابات التجربة
- [ ] فحص OWASP ZAP ومعالجة نتائجه
- [ ] مراجعة قانونية للشروط والخصوصية
- [ ] نسخ احتياطي مجدول
